enum net_t
    {
    //Clients connection
    client_send_socket, client_set_name, client_name_accepted, client_name_rejected, client_joined, client_left,
    send_full_level,
    actions_wait, actions_run, minigame_start, minigame_effects,
    COUNT
    };
